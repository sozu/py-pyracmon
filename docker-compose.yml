services:
  pyracmon:
    build:
      context: ./dev
      args:
        UID: ${UID:-1000}
        GID: ${GID:-1000}
    image: pyracmon:develop
    tty: true
    volumes:
      - ./dev:/home/dev
    container_name: pyracmon
  postgres:
    image: postgres:10
    ports:
      - ${PYRACMON_POSTGRES_PORT:-35432}:5432
    environment:
      POSTGRES_PASSWORD: postgres
      PGDATA: /var/lib/postgresql/data/postgres
    volumes:
      - pyracmon_postgres:/var/lib/postgresql/data/postgres
      - ./postgresql/initdb.d:/docker-entrypoint-initdb.d
    container_name: pyracmon_postgres
  mysql:
    image: mysql:8
    ports:
      - ${PYRACMON_MYSQL_PORT:-33306}:3306
    environment:
      MYSQL_ROOT_PASSWORD: root
    volumes:
      - pyracmon_mysql:/var/lib/mysql
      - ./mysql/initdb.d:/docker-entrypoint-initdb.d
      - ./mysql/conf.d:/etc/mysql/conf.d
volumes:
  pyracmon_postgres:
  pyracmon_mysql: